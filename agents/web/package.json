{
  "name": "ferminux-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/gen-config.mjs && vite",
    "build": "node scripts/gen-config.mjs && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.3"
  }
}
