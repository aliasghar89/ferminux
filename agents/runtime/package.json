{
  "name": "ferminux-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Reference Ferminux Network agent runtime: registers, serves an agent card, and fulfills escrow jobs.",
  "type": "module",
  "bin": {
    "ferminux-agent": "./dist/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "clean": "rm -rf dist",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@ferminux/agent": "file:../sdk",
    "fastify": "^5.0.0",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^7.0.2"
  },
  "license": "MIT"
}
