<?xml version="1.0" encoding="utf-8"?>
<!--
  devnet flavour ONLY (anvil tests on the emulator): plain http to the host machine
  (10.0.2.2 from the Android emulator) and to localhost for `adb reverse`. Everything else
  stays HTTPS-only with system CAs, exactly as in the shipped build.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
